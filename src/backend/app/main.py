# FastAPI Application Entry Point
# This is the main file that starts the FastAPI application
# - Initializes the FastAPI app instance
# - Sets up CORS middleware for frontend communication
# - Includes all API routers (auth, users, files, conversions)
# - Configures startup and shutdown events
# - Handles global exception handlers
# - Sets up middleware for logging and rate limiting
